import { PuntoRetiroService } from './punto-retiro-service';

describe('PuntoRetiroService', () => {
  it('should create an instance', () => {
    expect(new PuntoRetiroService()).toBeTruthy();
  });
});
